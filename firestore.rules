rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Temporary development rules - allow all reads and writes
    // TODO: Replace with proper security rules before production
    match /{document=**} {
      allow read, write;
    }
  }
}